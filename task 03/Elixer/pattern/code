n = String.to_integer(IO.gets("Enter an odd number: "))

for i <- 0..div(n, 2) do
  IO.puts String.duplicate(" ", div(n, 2) - i) <> String.duplicate("*", 2*i + 1)
end

for i <- (div(n, 2)-1)..0 do
  IO.puts String.duplicate(" ", div(n, 2) - i) <> String.duplicate("*", 2*i + 1)
end
